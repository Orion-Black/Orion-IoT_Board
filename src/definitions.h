#ifndef DEFINITIONS_H
#define DEFINITIONS_H

/* =======================
   ESTADOS UI (Global)
   ======================= */
enum UIState {
  UI_MAIN_MENU,
  UI_CONFIG_MENU,
  UI_WIFI_SCAN,
  UI_WIFI_PASSWORD,
  UI_LOCAL_MODE,
  UI_CLOUD_MODE,
  UI_TEST_MODE
};

#endif